<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SwiPixel" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/almost_black</item>
        <item name="colorOnPrimary">@color/off_white</item>
        <item name="backgroundColor">@color/off_white</item>
        <item name="colorOnBackground">@color/almost_black</item>
        <!-- Secondary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
        <item name="android:colorBackground">?attr/backgroundColor</item>
    </style>
</resources>